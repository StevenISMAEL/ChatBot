services:
  - type: web
    name: inimeg-sports-2-0
    env: node
    plan: free
    buildCommand: 'npm install && npm run build'
    startCommand: 'npm run start'
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: PORT
        value: 3000
